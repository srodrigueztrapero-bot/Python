### modules ###

import module

module.sum(5, 10, 15)
module.printValue("Hello, World!")

from module import sum, printValue
sumValue = sum(20, 30, 40)
printValue("Holi Python")

import math

print(math.pi)
print(math.pow(2, 3))